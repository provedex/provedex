# Provedex Node binding

Status: not built yet.

Plan:

- napi-rs wrapper around `provedex-core`.
- Publishes `@provedex/core` to npm.
- API: `signEvent(payload, parentHash, keypair)`, `verifyChain(events)`, `SigningKeypair.generate()`.
- TypeScript types generated by napi-rs.
- Native binaries for macOS arm64, macOS x86_64, linux x86_64, linux arm64, windows x86_64.
