# Voice scribe example

End-to-end voice agent demo. Identical pipeline to the live demo server in `crates/provedex-server/`, packaged as a standalone runnable example.

Until this is fleshed out, run the demo server directly:

```
cargo run -p provedex-server --features demo
```

Open `http://localhost:3000`.
